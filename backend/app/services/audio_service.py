class AudioService:
    def transcribe(self, audio_file) -> str:
        # Mock implementation: In reality, use Whisper
        return "This is the transcribed text from the classroom audio."

audio_service = AudioService()
